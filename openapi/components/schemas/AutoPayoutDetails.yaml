description: | 
  Information about the payout. 
  
type: object
properties:
  currency:
    $ref: 'Currency.yaml'
  schedule:
    $ref: 'Schedule.yaml'
  bankaccount_id:
    $ref: 'BankAccountID.yaml'
  min_amount:
    $ref: 'MinAmount.yaml'
  min_balance_amount:
    $ref: 'MinBalanceAmount.yaml'