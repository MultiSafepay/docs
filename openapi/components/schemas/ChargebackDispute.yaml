description: Information relating to a chargeback dispute.
type: object
properties:
  merchant_response_date:
    $import: 'MerchantNewDate.yaml'
  status:
    $import: 'ChargebackStatus.yaml'
  reason_code:
    $import: 'ChargebackReasonCode.yaml'
  reason_description:
    $import: 'ChargebackReasonCodeDescription.yaml'