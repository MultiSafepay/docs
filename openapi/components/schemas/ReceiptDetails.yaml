description: |
  Information about the receipt.

  The structure of the response may differ depending on type of transaction.
type: object
properties:
  merchant:
    $ref: 'ReceiptMerchant.yaml'
  order:
    $ref: 'ReceiptOrder.yaml'
  payment:
    $ref: 'ReceiptPayment.yaml'
  printed_on:
    $ref: 'ReceiptPrintedOn.yaml'
  related_transactions:
    type: array
    items:
      $ref: 'ReceiptRelatedTransactions.yaml'

 