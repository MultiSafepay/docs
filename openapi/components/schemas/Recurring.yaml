description: Additional settings for [recurring payments](https://docs.multisafepay.com/docs/recurring-payments).
type: object 
properties:
  model:
    $import: 'RecurringModel.yaml'
  id:
    $import: 'RecurringId.yaml'
