description: | 
  Information about the cancelled order.
type: object
properties:
  costs:
    description: The fees for the transaction.
  created:
    $ref: 'Created.yaml'
  custom_info:
    $ref: 'CustomInfo.yaml'
  fastcheckout:
    $ref: 'FastCheckout.yaml'
  financial_status:
    $ref: 'FinancialStatus.yaml'
  modified:
    $ref: 'Modified.yaml'
  payment_details:
    description: Information about the payment.
    type: object
  payment_methods:
    description: Information about the payment method.
    type: object
  status:
    $ref: 'Status.yaml'