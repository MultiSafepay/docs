description: The fees for the transaction.
type: object
properties:
  amount:
   $ref: 'Amount.yaml'
  description:
    $ref: 'Description.yaml'
  type:
    type: string
    enum: [SYSTEM, internal]
    example: "internal"
  currency:
    $ref: 'Currency.yaml'
  status:
   $ref: 'Status.yaml'