description: Refund (BNPL) order
type: object
properties:
  success:
    $ref: '../schemas/Success.yaml'
  data: 
    description: Information about the refund.
    type: object
    properties:
      transaction_id:
        $ref: '../schemas/TransactionId.yaml'
      refund_id:
        $ref: '../schemas/RefundId.yaml'