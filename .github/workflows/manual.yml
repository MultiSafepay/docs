name: Delete branch
on:
  workflow_dispatch:
  workflow_call:
jobs:
  delete-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Delete branch
        run: git push --delete origin $GITHUB_REF_NAME
